-r requirements.txt
transformers
# torch ставится отдельно (в Docker см. backend/Dockerfile: CPU-only с pytorch.org).
# Локально: pip install torch --index-url https://download.pytorch.org/whl/cpu
pandas
scikit-learn
joblib
